/**
 * One original Boneyard figure is the physical art for every side. Opponent identities are
 * FightLab colorways over that art, so no third-party figure or cosmetic is shipped.
 */
export const PLAYER_FIGURE = "runner";

/** Every figure the game draws, so a build can serve each one. */
export const FIGURES: readonly string[] = Object.freeze([PLAYER_FIGURE]);
